name: MoneySJobs
main: net.comorevi.np.moneys.Jobs
version: 1.0.0
load: POSTWORLD
author: popkechupki
api: ["1.0.0"]
description: "Job plugin for MoneySAPI"
depend:
  - FormAPI
  - MoneySAPI

permissions:
  moneys:
    children:
      moneys.jobs:
        children:
          moneys.jobs.jobs:
            default: true